const themeProvider = {
    getTheme: () => ({
        primaryColor: 'red',
        fontSize: 11,
        backgroundColor:'white'
    })
};

// update themes

export default themeProvider;

// need a bump