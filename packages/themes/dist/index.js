'use strict';

var themeProvider = {
    getTheme: function getTheme() {
        return {
            primaryColor: 'red',
            fontSize: 11,
            backgroundColor: 'white'
        };
    }
};

module.exports = themeProvider;
