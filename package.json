{
  "name": "lerna-repo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean":"lerna clean --yes &&find packages -name 'dist' |xargs rm -rf ",
    "build":"lerna bootstrap --hoist",
    "pusblish":"lerna publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "lerna": "^2.9.0",
    "yargs": "^11.0.0"
  }
}
